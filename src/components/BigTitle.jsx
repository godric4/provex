const BigTitle = ({ label }) => {
  return (
    <h2 className='text-center font-cormorant mt-1 text-[1.8rem] italic capitalize font-semibold'>
      {label}
    </h2>
  );
};

export default BigTitle;
